/**
 * Prototype types for capstone project showcasing (Capstone 2 & 3).
 * Prototypes demonstrate system development progress via images, videos, or external links.
 */
export const PROTOTYPE_TYPES = Object.freeze({
  IMAGE: 'image',
  VIDEO: 'video',
  LINK: 'link',
});

export const PROTOTYPE_TYPE_VALUES = Object.values(PROTOTYPE_TYPES);
