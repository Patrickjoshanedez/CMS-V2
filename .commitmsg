feat: add configurable system settings and audit trail (Sprint 13)

- SystemSettings module: singleton config for plagiarism threshold,
  system name, maintenance mode (FR-INS-05)
- AuditLog module: full audit trail with query, filter, pagination,
  and entity history endpoints (FR-16, NFR-07)
- auditLog middleware wired to 23 route actions across 6 modules
- project.service.js uses configurable threshold from SystemSettings
- Client: SystemSettingsForm, AuditLogPage, services, hooks, routing
- Tests: 26 settings tests + 22 audit tests (315 total, all green)

Closes: FR-INS-05, FR-16, NFR-07
