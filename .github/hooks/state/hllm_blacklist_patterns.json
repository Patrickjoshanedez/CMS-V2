{
  "version": "1.0.0",
  "description": "Central blacklist regex rules for HLLM preflight hook.",
  "patterns": [
    {
      "id": "no-test-skip-bypass",
      "pattern": "\\b(?:it|test|describe)\\.skip\\s*\\(",
      "reason": "Do not bypass failing tests by skipping them. Fix root cause instead."
    },
    {
      "id": "no-hard-reset",
      "pattern": "git\\s+reset\\s+--hard",
      "reason": "Destructive git history rewrites are forbidden in autonomous fix flows."
    },
    {
      "id": "no-checkout-discard",
      "pattern": "git\\s+checkout\\s+--",
      "reason": "Do not discard potentially relevant local work during automated repair."
    }
  ]
}
